rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow read/write access to anyone who is authenticated (including anonymous users)
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
